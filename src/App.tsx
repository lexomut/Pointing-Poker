import React from 'react';
import MainPage from './MainPage/MainPage';
import './App.css';

const App: React.FC = () => {
    return (
        <div>
            <MainPage />
        </div>
    );
};

export default App;
